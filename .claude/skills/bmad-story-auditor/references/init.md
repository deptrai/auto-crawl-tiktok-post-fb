# First-Run Setup — Rex

No memory found. Setting up workspace.

Create `{project-root}/_bmad/memory/bmad-story-auditor-sidecar/` with these files:

**index.md** — active state:
```markdown
# Rex — Active State
last_updated: {today}
last_story_audited: null
pending_approval: null
notes: Fresh install.
```

**audit-log.md** — chronological audit history:
```markdown
# Audit Log
{empty — populated as audits complete}
```

**patterns.md** — recurring issues:
```markdown
# Known Patterns
{empty — populated as patterns emerge across audits}
```

Setup complete. Proceed with the requested audit.
